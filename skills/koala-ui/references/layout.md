<!-- Generated by `npm run skill` from https://koala-ui.vercel.app/docs/components/layout.md. Do not edit: regenerate it. -->

# Layout

The application and page shell. One set of parts and two app shells that share it: plain, the classic rail-and-content layout, and docked, the default, with the rail on a recessed canvas.

```tsx
<Layout className="h-svh overflow-hidden">
  <LayoutSidebar>
    {/* docked automatically: no fill, no hairline, sized by the column */}
    <Sidebar aria-label="Main">
      <SidebarHeader>{/* workspace switcher */}</SidebarHeader>
      <SidebarContent>
        <SidebarGroup aria-label="Primary">
          <SidebarItem active><GearSix /> Settings</SidebarItem>
        </SidebarGroup>
      </SidebarContent>
      <SidebarFooter>{/* profile switcher */}</SidebarFooter>
    </Sidebar>
  </LayoutSidebar>

  <LayoutContent>            {/* the raised sheet, flush to the window edges */}
    <LayoutContainer>        {/* centered max-width column */}
      <LayoutHeader>         {/* breadcrumbs row at the top */}
        <Breadcrumb>
          <BreadcrumbList>
            <BreadcrumbItem>
              <BreadcrumbLink href="#">Billing</BreadcrumbLink>
            </BreadcrumbItem>
            <BreadcrumbSeparator />
            <BreadcrumbItem>
              <BreadcrumbPage>Invoices</BreadcrumbPage>
            </BreadcrumbItem>
          </BreadcrumbList>
        </Breadcrumb>
      </LayoutHeader>

      <h1 className="text-2xl font-semibold">General settings</h1>

      {/* settings sections: the header column beside the field column */}
      <FormSection layout="split" density="comfortable" className="border-t border-border py-8">
        <FormSectionHeader>
          <FormSectionTitle className="text-base">Profile</FormSectionTitle>
          <FormSectionDescription>This information is shown to your teammates.</FormSectionDescription>
        </FormSectionHeader>
        <FormSectionBody density="comfortable">
          <Field>
            <FieldLabel>Full name</FieldLabel>
            <InputRoot><InputField defaultValue="Mara Okonkwo" /></InputRoot>
          </Field>
        </FormSectionBody>
      </FormSection>

      <FormActions bordered>
        <Button variant="ghost">Cancel</Button>
        <Button>Save changes</Button>
      </FormActions>
    </LayoutContainer>
  </LayoutContent>
</Layout>
```

## Installation

```bash
# One-time setup (tokens + lib helpers)
npx koalaui-cli init

# Add this component (its dependencies come along)
npx koalaui-cli add layout
```

Manual: run `npm install radix-ui tailwind-variants tailwind-merge`, then copy the source into `components/ui/layout/` and adjust the import paths to your project. Components pull `cn` from `lib/utils`, the `tv` wrapper from `lib/tv`, and (for multi-part components) `createContext` from `lib/create-context`.

## Usage

```tsx title="usage.tsx"
import {
  Layout, LayoutSidebar, LayoutContent,
  LayoutContainer, LayoutHeader,
} from "@/components/ui/layout"

export function AppShell({ children }: { children: React.ReactNode }) {
  return (
    <Layout className="h-svh overflow-hidden">
      <LayoutSidebar>{/* nav */}</LayoutSidebar>
      <LayoutContent>
        <LayoutContainer>
          <LayoutHeader>{/* breadcrumbs */}</LayoutHeader>
          {children}
        </LayoutContainer>
      </LayoutContent>
    </Layout>
  )
}
```

## Variants

There are two app shells and one reading shell. **The two app shells are the same layout** — same parts, same structure, same scroll model, rail · content — at two levels of dress. Switching between them changes nothing about your markup.

- **`plain` — the classic shell.** One background, one hairline, nothing raised and nothing recessed. It is defined by what it doesn’t do: the content paints no surface of its own, and the rail is left alone, keeping the card surface and edge hairline a `Sidebar` wears when it stands on its own. A rail in a plain shell and a rail with no shell look identical.
- **`docked` — the same shell, dressed. (Default.)** The rail recedes onto a `bg-canvas` floor and the content rises into a `bg-card` sheet, meeting along a seam with rounded leading corners. Still flush to all four window edges; still nothing floating.

```tsx
<Layout variant="plain">   {/* the classic shell; identical markup to docked */}
  <LayoutSidebar><Sidebar>…</Sidebar></LayoutSidebar>
  <LayoutContent>
    <LayoutContainer width="wide">{/* page */}</LayoutContainer>
  </LayoutContent>
</Layout>
```

`flat`, the third, is a different *job* rather than a different look. Content sits flat on the page, the rail and a right-hand `LayoutAside` are divided from it by hairlines, and — the part that matters — the **page scrolls as a whole**: the content is not its own scroll container, which is what lets a window scroll-spy drive an “on this page” list. Documentation, not apps. This very site runs on it.

What the set deliberately does *not* contain is a second *decorated* app shell: the floating card shadcn calls `inset`, content in a shadowed panel with the canvas showing as a gutter around it. Undressed versus dressed is a real axis; *dressed one way* versus *dressed another* is how a design system drifts. A screen that really wants it opts in per instance and owns the exception:

```tsx title="floating.tsx"
{/* An escape hatch, not a supported structure. */}
<LayoutContent className="m-2 rounded-2xl shadow-sm ring-1 ring-border lg:ml-0">
```

```tsx
<Layout variant="flat">
  <LayoutSidebar>{/* the docs rail */}</LayoutSidebar>
  <LayoutContent>
    <LayoutContainer width="narrow">
      {children}
    </LayoutContainer>
  </LayoutContent>
  <LayoutAside>{/* on-this-page nav */}</LayoutAside>
</Layout>
```

## Structures

The variants set how a shell is dressed. The *structure* is which parts you drop in, and there is no prop for it: put a `LayoutTopbar` first and the shell becomes a column with the band on top; put a `LayoutRail` before the rail and it becomes two tiers; put `LayoutPane`s in the content and the sheet splits into a list and its detail. The sheet then decorates whichever seam it actually has, so every arrangement below reads as one app rather than a stack of boxes.

## Anatomy

`Layout` is a flex row on the recessed `bg-canvas` floor. `LayoutContent` is the sheet: a `bg-card` surface running flush to the top, right, and bottom window edges, meeting the rail along a hairline with its leading corners rounded. `LayoutContainer` centers the column and `LayoutHeader` lays out the breadcrumbs row.

Space is distributed across three nested bands:

- **Canvas**: the recessed `bg-canvas` floor the `w-64` (256px) rail sits on. It exists as its own colour role because `--background` and `--card` are the same white in the light theme, so it could not express “one step below the sheet”.
- **Sheet**: the `bg-card` surface. Only the leading edge is decorated: a `border-l` hairline plus `rounded-tl-xl` where that seam meets the top of the window, so the canvas shows through as a single notch there. **The other three corners stay square.** The sheet runs to the bottom edge, and a rounded corner against the window floor reads as a mis-clipped panel rather than a deliberate one, which is why Linear, Discord, and Slack all round the top-left alone. Nest surfaces inside with a smaller, concentric radius (`the sheet’s radius − padding`).
- **Column**: `LayoutContainer` centers a max-width column (`mx-auto`) and sets the content padding. Horizontal padding is responsive: it steps up at `sm` and `lg` so content breathes on wider viewports.

```tsx
<Layout className="h-svh overflow-hidden">
  <LayoutSidebar>
    {/* docked automatically: no fill, no hairline, sized by the column */}
    <Sidebar aria-label="Main">
      <SidebarHeader>{/* workspace switcher */}</SidebarHeader>
      <SidebarContent>
        <SidebarGroup aria-label="Primary">
          <SidebarItem active><GearSix /> Settings</SidebarItem>
        </SidebarGroup>
      </SidebarContent>
      <SidebarFooter>{/* profile switcher */}</SidebarFooter>
    </Sidebar>
  </LayoutSidebar>

  <LayoutContent>            {/* the raised sheet, flush to the window edges */}
    <LayoutContainer>        {/* centered max-width column */}
      <LayoutHeader>         {/* breadcrumbs row at the top */}
        <Breadcrumb>
          <BreadcrumbList>
            <BreadcrumbItem>
              <BreadcrumbLink href="#">Billing</BreadcrumbLink>
            </BreadcrumbItem>
            <BreadcrumbSeparator />
            <BreadcrumbItem>
              <BreadcrumbPage>Invoices</BreadcrumbPage>
            </BreadcrumbItem>
          </BreadcrumbList>
        </Breadcrumb>
      </LayoutHeader>

      <h1 className="text-2xl font-semibold">General settings</h1>

      {/* settings sections: the header column beside the field column */}
      <FormSection layout="split" density="comfortable" className="border-t border-border py-8">
        <FormSectionHeader>
          <FormSectionTitle className="text-base">Profile</FormSectionTitle>
          <FormSectionDescription>This information is shown to your teammates.</FormSectionDescription>
        </FormSectionHeader>
        <FormSectionBody density="comfortable">
          <Field>
            <FieldLabel>Full name</FieldLabel>
            <InputRoot><InputField defaultValue="Mara Okonkwo" /></InputRoot>
          </Field>
        </FormSectionBody>
      </FormSection>

      <FormActions bordered>
        <Button variant="ghost">Cancel</Button>
        <Button>Save changes</Button>
      </FormActions>
    </LayoutContainer>
  </LayoutContent>
</Layout>
```

## Container width

`LayoutContainer` takes a `width`: `narrow` (`max-w-3xl`), `default` (`max-w-6xl`), `wide` (`max-w-7xl`), or `full`. These map to Tailwind’s native `max-w-*` scale: use `narrow` (768px) for single-column forms and reading, `default` (1152px) for standard dashboards and two-column settings, `wide` (1280px) for dense tables and grids, and `full` for edge-to-edge canvases.

```tsx
<LayoutContainer width="narrow">…</LayoutContainer>
```

## Page header

`PageHeader` is the title band that sits inside the column, below the breadcrumbs. The heading and its supporting text go in `PageHeaderContent`; the trailing `PageHeaderActions` holds page-level triggers (an outline secondary plus a primary). It stacks on phones and splits into a row at `sm`, so the actions never crowd a long title.

```tsx
<LayoutContainer width="wide">
  <LayoutHeader className="mb-4">
    <Breadcrumb>…</Breadcrumb>
  </LayoutHeader>

  <PageHeader>
    <PageHeaderContent>
      <PageHeaderHeading>Invoices</PageHeaderHeading>
      <PageHeaderDescription>
        Every charge across your workspace, with status and totals.
      </PageHeaderDescription>
    </PageHeaderContent>
    <PageHeaderActions>
      <Button variant="outline"><Export /> Export</Button>
      <Button><Plus /> New invoice</Button>
    </PageHeaderActions>
  </PageHeader>

  {/* page content */}
</LayoutContainer>
```

## Docked rail

The rail needs no configuration: nest a [`Sidebar`](https://koala-ui.vercel.app/docs/components/sidebar.md) inside `LayoutSidebar` and it comes out *docked*, sitting bare on the canvas beside the sheet instead of painting a second card on top of it. The column owns the surface, the width, and (in the `flat` shell) the dividing hairline; the rail owns its own chrome and padding. That split is what keeps the app shell free of `bg-transparent` / `border-r-0` patches at the call site.

It travels: `LayoutMobileSidebar` publishes the same contract, so the one rail reads correctly in the drawer too. A rail that genuinely wants its own surface opts out with `docked={false}`, or takes the `floating` card treatment shown under [Resizable panels](#resizable-panels).

```tsx title="docked.tsx"
{/* Before: the shell's look, faked at the call site */}
<LayoutSidebar className="p-0">
  <Sidebar className="w-full border-r-0 bg-transparent">…</Sidebar>
</LayoutSidebar>

{/* Now: the shell owns it */}
<LayoutSidebar>
  <Sidebar>…</Sidebar>
</LayoutSidebar>
```

## Collapsible rail

The shell owns the rail’s collapse state, so a screen doesn’t have to wire it. Drop a `LayoutSidebarTrigger` in the header and the rail folds to a 48px icon column and back; ⌘B (Ctrl+B on Windows and Linux) does the same from anywhere, and stands down while you’re typing in a field or an editor, where the chord means bold. Only the rail column narrows: the sheet stays put against the window edges and simply gains the width.

It is uncontrolled by default. Pass `defaultCollapsed` for a different starting state, or `collapsed` + `onCollapsedChange` to drive it yourself and persist the choice per user. `useLayoutSidebar()` reads and flips the same state from any descendant, for a bespoke trigger. Give each `SidebarItem` a `label` so the icon rows keep an accessible name and a hover tooltip.

```tsx
<Layout>                        {/* owns the state; ⌘B by default */}
  <LayoutSidebar>
    <Sidebar>
      <SidebarItem label="Overview"><SquaresFour /> Overview</SidebarItem>
    </Sidebar>
  </LayoutSidebar>

  <LayoutContent>
    <LayoutContainer>
      <LayoutHeader>
        <div className="flex items-center gap-2">
          <LayoutSidebarTrigger />
          <Breadcrumb>…</Breadcrumb>
        </div>
      </LayoutHeader>
    </LayoutContainer>
  </LayoutContent>
</Layout>
```

## Top navigation

For an app with a handful of top-level sections, put the navigation in a band across the top instead of a rail. Drop a `LayoutTopbar` in as the first child and the shell turns into a column: the band spans the window, and the sheet fills the rest and scrolls on its own. The band hosts a [`Navbar`](https://koala-ui.vercel.app/docs/components/navbar.md) the way `LayoutSidebar` hosts a `Sidebar`: the bar docks onto it, painting no fill and no bottom rule of its own, and its row runs the full width instead of the marketing column.

The seam turns horizontal. The band sits on the canvas, and the sheet under it draws a top hairline and keeps its corners square: a seam that runs wall to wall has no corner of its own to round. Below `md` the links fold into the Navbar’s own menu.

```tsx
<Layout className="h-svh overflow-hidden">
  <LayoutTopbar>
    {/* docked automatically: no fill, no bottom rule, full-width row */}
    <Navbar density="compact" linkStyle="underline">
      <NavbarInner>
        <NavbarBrand href="/"><BrandMark /></NavbarBrand>
        <NavbarNav>
          <NavbarLink href="/">Overview</NavbarLink>
          <NavbarLink href="/invoices" active>Invoices</NavbarLink>
        </NavbarNav>
        <NavbarSpacer />
        <NavbarSearch className="hidden max-w-56 md:flex" />
        <NavbarActions>
          <Button variant="ghost" iconOnly aria-label="Notifications"><Bell /></Button>
          <AccountMenu />
        </NavbarActions>
        <NavbarMobileToggle />
      </NavbarInner>
      <NavbarMobileMenu>…</NavbarMobileMenu>
    </Navbar>
  </LayoutTopbar>

  <LayoutContent>              {/* the sheet: a top seam, square corners */}
    <LayoutContainer width="wide">…</LayoutContainer>
  </LayoutContent>
</Layout>
```

### With tabs

The band is a column, so a second row stacks under the first. Put both rows in one `Navbar` (one `<header>` landmark, two `NavbarInner`s) with `linkStyle="underline"`: the tab indicator lands right on the seam, so the current tab reads as the doorway into the sheet below it. The tab row never folds into a menu; on a phone it scrolls sideways.

```tsx
<LayoutTopbar>
  <Navbar density="compact" linkStyle="underline">
    <NavbarInner>
      <NavbarBrand href="/"><BrandMark /></NavbarBrand>
      <span aria-hidden className="text-lg font-light text-border">/</span>
      <ProjectSwitcher />
      <NavbarSpacer />
      <NavbarActions>…</NavbarActions>
    </NavbarInner>

    {/* the tab row: -ml-2 lines the first label up with the brand */}
    <NavbarInner className="h-11">
      <NavbarNav aria-label="Billing" className="-ml-2 flex min-w-0 overflow-x-auto">
        <NavbarLink href="/billing">Overview</NavbarLink>
        <NavbarLink href="/billing/invoices" active>Invoices</NavbarLink>
      </NavbarNav>
    </NavbarInner>
  </Navbar>
</LayoutTopbar>
```

## Top bar over the rail

The Slack, YouTube and IDE shell: a band across the whole window for the brand, a global search and the account, with the rail and the sheet side by side underneath. Put the rail and the sheet in a `LayoutBody`, the row that fills whatever the band leaves. The rail stops being viewport-tall and simply stretches to that row.

Here the sheet has two seams, the band above it and the rail beside it. It draws both hairlines and rounds the single corner where they meet. The rail toggle moves up into the band, lined up with the rail’s icons, so it still reads as the rail’s own control.

```tsx
<Layout className="h-svh overflow-hidden">
  <LayoutTopbar>
    <Navbar density="compact">
      <NavbarInner className="grid grid-cols-[minmax(0,1fr)_minmax(0,28rem)_minmax(0,1fr)] pl-2">
        <div className="flex items-center gap-1">
          <LayoutSidebarTrigger />
          <NavbarBrand href="/"><BrandMark /></NavbarBrand>
        </div>
        <NavbarSearch placeholder="Search Koala" className="max-w-none" />
        <NavbarActions className="justify-self-end">…</NavbarActions>
      </NavbarInner>
    </Navbar>
  </LayoutTopbar>

  <LayoutBody>                 {/* the row under the band */}
    <LayoutSidebar>
      <Sidebar aria-label="Main">…</Sidebar>
    </LayoutSidebar>
    <LayoutContent>            {/* two seams, one rounded corner */}
      <LayoutContainer width="wide">…</LayoutContainer>
    </LayoutContent>
  </LayoutBody>
</Layout>
```

## Two-tier rail

When the app is several products in one (a mail, a calendar, a billing module), the rail splits in two. A `LayoutRail` strip on the far left switches between modules, and the `LayoutSidebar` beside it holds the open module’s own navigation. Put a plain `Sidebar` in the strip and it comes out as the 48px icon rail, since the strip hands it `collapsed`; give every row a `label` for its tooltip and accessible name. A hairline divides the two columns, because two canvas columns with no surface change between them would run together into one wide rail.

Pass `collapsible="offcanvas"` to the panel. The strip already is the icon column, so collapsing the panel slides it shut instead of folding it into a second strip. It slides out whole, labels and all, and leaves the tab order once it is gone.

```tsx
<Layout className="h-svh overflow-hidden">
  <LayoutRail>                     {/* the module strip: an icon rail, no props */}
    <Sidebar aria-label="Apps">
      <SidebarHeader>{/* workspace switcher */}</SidebarHeader>
      <SidebarContent>
        <SidebarGroup aria-label="Apps">
          <SidebarItem label="Home"><House /> Home</SidebarItem>
          <SidebarItem active label="Billing"><Receipt /> Billing</SidebarItem>
        </SidebarGroup>
      </SidebarContent>
      <SidebarFooter>{/* account switcher */}</SidebarFooter>
    </Sidebar>
  </LayoutRail>

  <LayoutSidebar collapsible="offcanvas">   {/* the open module's nav */}
    <Sidebar aria-label="Billing">…</Sidebar>
  </LayoutSidebar>

  <LayoutContent>
    <LayoutContainer width="wide">
      <LayoutHeader>
        <LayoutSidebarTrigger />   {/* slides the panel; the strip stays */}
        <Breadcrumb>…</Breadcrumb>
      </LayoutHeader>
      …
    </LayoutContainer>
  </LayoutContent>
</Layout>
```

## List and detail

Mail, chat and support queues are one screen split into columns: a list, the item you opened, sometimes an inspector beside it. Put `LayoutPane`s straight inside `LayoutContent` and the sheet lays them out as a row. Each pane scrolls on its own, and every pane after the first draws the hairline that divides it from the one before.

- **`width`** is `sm` (256px), `md` (320px), `lg` (384px) or `fill`, the default, which takes whatever the fixed panes leave. Give exactly one pane in the row `fill`.
- **`LayoutPaneHeader`** is the bar across the top of a pane: the list’s title, the reading pane’s actions. Every pane header is the same height, so side by side their bottom rules meet in one straight line across the seams.
- **The panes are one surface.** The rail-to-sheet seam is a surface change and keeps its rounded corner; the seams between panes are divisions *within* the sheet, so they get a hairline and nothing else.

```tsx
{/* the rail starts as icons: the reading pane gets the width */}
<Layout defaultCollapsed className="h-svh overflow-hidden">
  <LayoutSidebar>
    <Sidebar aria-label="Mail">…folders…</Sidebar>
  </LayoutSidebar>

  <LayoutContent>              {/* holding panes turns the sheet into a row */}
    <LayoutPane width="md" aria-label="Inbox">
      <LayoutPaneHeader>
        <h2 className="text-sm font-semibold">Inbox</h2>
        <Button variant="ghost" size="sm" iconOnly className="ml-auto" aria-label="Mark all as read">
          <CheckCircle />
        </Button>
      </LayoutPaneHeader>
      <List variant="plain" className="px-4 py-2">…messages…</List>
    </LayoutPane>

    <LayoutPane aria-label="Message">    {/* width="fill" by default */}
      <LayoutPaneHeader>…actions…</LayoutPaneHeader>
      <article className="p-6">…</article>
    </LayoutPane>
  </LayoutContent>
</Layout>
```

### With an inspector

A third pane on the right holds the details of the open item: the requester, the status, the fields an agent edits. It continues the same sheet, so it is just one more `LayoutPane`. Panes compose with any structure: here the queues ride in a top band, which leaves the sheet its full width for three columns. For panes the reader can drag wider, compose the same parts inside [Resizable panels](#resizable-panels).

```tsx
<Layout className="h-svh overflow-hidden">
  <LayoutTopbar>
    <Navbar density="compact" linkStyle="underline">…queues…</Navbar>
  </LayoutTopbar>

  <LayoutContent>
    <LayoutPane width="sm" aria-label="Open tickets">
      <LayoutPaneHeader>…</LayoutPaneHeader>
      <List variant="plain" className="px-4 py-2">…</List>
    </LayoutPane>

    <LayoutPane aria-label="Conversation">
      <LayoutPaneHeader>
        <h2 className="truncate text-sm font-semibold">Can't download my invoice PDF</h2>
        <Button variant="outline" size="sm" className="ml-auto"><CheckCircle /> Resolve</Button>
      </LayoutPaneHeader>
      …thread · reply box…
    </LayoutPane>

    <LayoutPane width="sm" aria-label="Ticket details">   {/* the inspector */}
      <LayoutPaneHeader>…</LayoutPaneHeader>
      <DescriptionList>…</DescriptionList>
    </LayoutPane>
  </LayoutContent>
</Layout>
```

## Sticky header

Pass `sticky` to `LayoutHeader` to pin the breadcrumbs and actions to the top of the scrolling sheet. It bleeds to the column edges with a translucent, blurred fill (`bg-card/85` + `backdrop-blur-sm`) and the inset margins track the column’s responsive padding, so it lines up at every breakpoint.

```tsx
<LayoutHeader sticky>
  <Breadcrumb>…</Breadcrumb>
  <Button size="sm"><Plus /> New invoice</Button>
</LayoutHeader>
```

## Mobile navigation

`LayoutSidebar` is hidden below `lg`. On small screens, put a `LayoutMobileBar` at the top of `LayoutContent` and drop a `LayoutMobileSidebar` inside it: a hamburger that opens the same `Sidebar` in a left [Drawer](https://koala-ui.vercel.app/docs/components/drawer.md), so one rail serves both layouts. The demo below is framed as a phone and forces the mobile bar on at every width.

```tsx
<LayoutContent>
  <LayoutMobileBar>
    <LayoutMobileSidebar>
      <Sidebar>…</Sidebar>
    </LayoutMobileSidebar>
    <Brand />
  </LayoutMobileBar>

  <LayoutContainer>{/* page */}</LayoutContainer>
</LayoutContent>
```

## Resizable panels

By default the rail is a constant (`w-64`). Compose the shell with [Resizable](https://koala-ui.vercel.app/docs/components/resizable.md) to make it a drag instead: wrap the columns in a `ResizablePanelGroup` on a fixed-height shell (`h-svh overflow-hidden`), and each `ResizablePanel` takes over the width the recipe used to fix. The `Layout` parts go inside the panels unchanged.

Two rules carry across every arrangement below. **Give every pane except one hard `px` bounds** (`minSize` / `maxSize`): the unbounded one absorbs the difference, so nothing collapses into an unusable sliver at small window sizes. And **the handle is a 1px `bg-border` line, so it *is* the seam**: the sheet drops its own hairline (`border-l-0`) wherever a handle sits beside it, or the two stack up and read as 2px. Its rounded leading corners stay.

```tsx
<Layout className="h-svh overflow-hidden">
  <ResizablePanelGroup>
    {/* the rail, on the canvas. collapsible: snaps shut past half its min */}
    <ResizablePanel defaultSize={26} minSize="220px" maxSize="380px" collapsible>
      <LayoutSidebar className="flex h-full w-full">
        <Sidebar>…</Sidebar>
      </LayoutSidebar>
    </ResizablePanel>

    <ResizableHandle withHandle />

    {/* the sheet. border-l-0: the handle already draws that line */}
    <ResizablePanel defaultSize={74}>
      <LayoutContent className="h-full border-l-0">
        <LayoutContainer width="wide">{/* page */}</LayoutContainer>
      </LayoutContent>
    </ResizablePanel>
  </ResizablePanelGroup>
</Layout>
```

### Floating rail

The one place a card still floats in the docked shell. Pass `floating` to the [Sidebar](https://koala-ui.vercel.app/docs/components/sidebar.md) and it detaches into its own surface (outline, radius, shadow) on the canvas, while the content stays the flush sheet, so the two read as distinct objects across the seam. The rail’s panel needs padding for that float to be visible: with none, the card butts against the window edge and there is nothing for it to float above.

```tsx
{/* p-2 is the air the floating card needs to read as detached */}
<ResizablePanel defaultSize={26} minSize="220px" maxSize="380px" className="p-2">
  <LayoutSidebar className="flex h-full w-full">
    <Sidebar floating>…</Sidebar>
  </LayoutSidebar>
</ResizablePanel>
```

### Three panes

The detail-view dashboard: a rail, the content, and a right-hand inspector, with a draggable seam between each pair. Each handle resizes only the two panes it sits between, so the three rebalance independently.

What makes this read correctly is that **the two seams are not the same kind of edge**, and treating them alike is the usual mistake:

- **Seam 1, canvas → sheet.** The rail sits on the recessed `bg-canvas` floor and the content is the raised `bg-card` sheet. This is the shell’s real edge, so the sheet keeps its rounded leading corners here: it is a surface change.
- **Seam 2, sheet → sheet.** The inspector *continues* the same sheet: same `bg-card`, no radius, no shadow, no second gutter. It is a division *within* one surface, not a boundary between two. Give it a card of its own and the screen suddenly reads as three floating objects instead of one app.

Both outer panes are `collapsible` and bounded; the middle carries no `px` bounds, so it is the pane that absorbs whatever the other two give up.

```tsx
<ResizablePanelGroup>
  {/* rail — on the canvas */}
  <ResizablePanel collapsible minSize="200px" maxSize="320px">
    <LayoutSidebar className="flex h-full w-full"><Sidebar>…</Sidebar></LayoutSidebar>
  </ResizablePanel>

  <ResizableHandle withHandle />        {/* seam 1: canvas → sheet */}

  {/* content — the sheet. Unbounded: absorbs the difference */}
  <ResizablePanel defaultSize={56}>
    <LayoutContent className="h-full border-l-0">…</LayoutContent>
  </ResizablePanel>

  <ResizableHandle withHandle />        {/* seam 2: sheet → sheet */}

  {/* inspector — the SAME sheet continued: bg-card, no radius, no shadow */}
  <ResizablePanel collapsible minSize="260px" maxSize="380px">
    <div className="h-full bg-card p-4 [--surface:var(--card)]">…</div>
  </ResizablePanel>
</ResizablePanelGroup>
```

## Split layout

`SplitLayout` is a second shell in this module for the full-viewport auth, onboarding, and marketing screens. It fills the viewport (`min-h-svh`) and splits into two columns at `lg`. One pane carries the content (a centered `SplitPaneBody` column); the other, `SplitMedia`, is a full-bleed image panel with an optional `SplitMediaOverlay` for a caption or testimonial. Below `lg` the grid collapses to a single column and the media pane drops away, so the form fills the screen on phones.

```tsx
<SplitLayout>
  <SplitPane>                 {/* content side: centers a column */}
    <SplitPaneBody>           {/* the centered max-width column */}
      <Brand />
      <h1>Create an account</h1>

      <Button variant="outline" className="w-full"><GoogleLogo /> Google</Button>
      <Divider>or continue with email</Divider>

      <Form onSubmit={handleSubmit}>
        <Field>
          <FieldLabel>Email address</FieldLabel>
          <InputRoot><InputField type="email" /></InputRoot>
        </Field>
        <FormActions align="stack">
          <Button type="submit">Create account</Button>
        </FormActions>
      </Form>
    </SplitPaneBody>
  </SplitPane>

  <SplitMedia>                {/* full-bleed image side, hidden below lg */}
    <img className="absolute inset-0 size-full object-cover" src="/cover.jpg" alt="" />
    <SplitMediaOverlay>
      <p>Koala UI let us ship a polished product in days.</p>
    </SplitMediaOverlay>
  </SplitMedia>
</SplitLayout>
```

### Media side & ratio

Which side the media sits on is just DOM order. Put `SplitMedia` first for an image-left layout. The `ratio` prop divides the columns: `even` (the 50/50 default), `start`, or `end` to give the leading or trailing pane the larger 3:2 share. Here the media leads with `start` so the hero panel dominates.

```tsx
<SplitLayout ratio="start">
  <SplitMedia>                {/* media first, sits on the left */}
    <img className="absolute inset-0 size-full object-cover" src="/cover.jpg" alt="" />
    <SplitMediaOverlay className="justify-between">
      <Brand className="text-white" />
      <p>Welcome back to your workspace.</p>
    </SplitMediaOverlay>
  </SplitMedia>

  <SplitPane>
    <SplitPaneBody>{/* sign-in form */}</SplitPaneBody>
  </SplitPane>
</SplitLayout>
```

## Density

`density` on `Layout` flows through context to the rail padding, the column’s inner padding, and the header gap. `compact` is the default (app UI); `comfortable` is roomier. Either way the column’s horizontal padding is responsive (`px-4 sm:px-6 lg:px-8` in `compact`), so the gutter grows with the viewport. Set it once here or for a whole subtree with `DensityProvider`. See [Density](https://koala-ui.vercel.app/docs/foundations/density.md).

```tsx
<Layout density="comfortable">…</Layout>
```

## FAQ

### Which variant should I use?

For an app or dashboard, pick between the two app shells: `docked` (the default) fills the window edge to edge with the rail on the recessed canvas and the content as the raised sheet, the way Linear, Discord, and Slack are built; `plain` is that same shell undressed, one background and one hairline, with the rail keeping its own standalone chrome. They share the same markup, so switching is a one-word change. `flat` is not an app shell at all: use it for docs and reading, where content sits flat on the page with a right-hand `LayoutAside` and the *window* scrolls as a whole so a scroll-spy can work.

### Is there a floating / inset shell?

No, on purpose. `plain` versus `docked` is undressed versus dressed, a real axis. A floating card shell would be dressed a rival way for the same job, which is how a system drifts. If one screen truly needs it, put `m-2 rounded-2xl shadow-sm ring-1 ring-border lg:ml-0` on that `LayoutContent` and own the exception.

### Why is there a --canvas colour role instead of just using --background?

Because in the light theme `--background` and `--card` are both pure white. A docked shell separates the rail from the content with surfaces rather than a shadow, so it needs a floor that is genuinely one step below the sheet. `--canvas` is that role: a whisper of grey in light, and equal to the page background in the dark themes, where `--card` already sits above it.

### Why doesn't LayoutSidebar show on mobile?

`LayoutSidebar` is hidden below `lg` by design. On small screens, put a `LayoutMobileBar` at the top of `LayoutContent` with a `LayoutMobileSidebar`, which opens the same `Sidebar` in a left Drawer so one rail serves both layouts.

### How do I control the content column width?

Set `width` on `LayoutContainer`: `narrow` (768px) for forms and reading, `default` (1152px) for dashboards, `wide` (1280px) for dense tables, or `full` for edge-to-edge. These map to Tailwind's native `max-w-*` scale.

### How do I keep the breadcrumb header in view while scrolling?

Pass `sticky` to `LayoutHeader`. It pins to the top of the scrolling sheet, bleeds to the column edges with a translucent blurred fill, and its inset margins track the column's responsive padding so it lines up at every breakpoint.

### Do I need to restyle the Sidebar to sit in the shell?

No. Nesting a `Sidebar` in a `LayoutSidebar` docks it: the shell owns the surface, the width, and (in `flat`) the dividing hairline, so the rail drops its own card fill and hairline. `LayoutMobileSidebar` publishes the same contract, so one rail reads correctly on desktop and in the drawer. Pass `docked={false}` to opt a rail out.

### How do I collapse the sidebar to icons?

The shell owns that state. Drop a `LayoutSidebarTrigger` in the header, or press ⌘B (Ctrl+B), and the rail folds to a 48px icon column while the sheet stays put against the window edges and gains the width. It's uncontrolled by default: pass `defaultCollapsed`, or `collapsed` + `onCollapsedChange` to drive and persist it, and `useLayoutSidebar()` to read or flip it from a custom trigger. Give each `SidebarItem` a `label` so the icon rows keep a name and a tooltip.

### Can I turn off the ⌘B shortcut?

Pass `collapseShortcut={false}`. The binding is document-level and on by default, which is right for an app with one shell; turn it off when a page mounts several `Layout`s (this documentation page does) or when ⌘B already means something else there. It also stands down on its own while you're typing in a field or a rich-text editor.

### How do I make the sidebar draggable instead of fixed-width?

Compose with Resizable: wrap the columns in a `ResizablePanelGroup` on a fixed-height shell (`h-svh overflow-hidden`). Each `ResizablePanel` now owns the width the recipe used to fix at `w-64`, and `collapsible` snaps the rail shut past half its min. Two things to get right: leave exactly one pane without `px` bounds so it absorbs the difference, and put `border-l-0` on the `LayoutContent` beside a handle, since the handle is already a 1px `bg-border` line and the two would stack up.

### In a three-pane layout, should the inspector be its own card?

No. The rail sits on the recessed canvas and the content is the raised sheet, so that first seam is a real surface change and the sheet keeps its rounded leading corners there. The inspector continues that same sheet: same `bg-card`, no radius, no shadow, no second gutter. The seam between them divides one surface rather than separating two. Giving the inspector its own card makes the screen read as three floating objects instead of one app.

### How do I put the navigation on top instead of in a rail?

Drop a `LayoutTopbar` in as the first child of `Layout` and put a `Navbar` inside it. The shell turns into a column on its own, with the band across the window and the sheet filling the rest, and the bar docks onto the band with no props. There is no `nav` prop to set: the structure is whichever parts you drop in. For a tab row, add a second `NavbarInner` to the same `Navbar`.

### Can I have a top bar and a rail at the same time?

Yes, that is the Slack or YouTube shell. Put the `LayoutTopbar` first, then a `LayoutBody` holding the `LayoutSidebar` and the `LayoutContent`. The sheet draws both seams, the band above and the rail beside it, and rounds the one corner where they meet. Move the `LayoutSidebarTrigger` into the band so the rail keeps its toggle.

### What is the difference between LayoutRail and LayoutSidebar?

`LayoutRail` is the thin module strip of a two-tier rail (Slack's workspaces, an IDE's activity bar): 48px wide, and the `Sidebar` inside it always renders as icons. `LayoutSidebar` is the rail proper, with the labelled navigation. Beside a strip, give the `LayoutSidebar` `collapsible="offcanvas"` so collapsing slides it shut instead of turning it into a second icon column.

### How do I build a mail or chat layout with a list and a reading pane?

Put two or more `LayoutPane`s straight inside `LayoutContent`. The sheet lays them out as a row, each pane scrolls on its own, and every pane after the first draws the dividing hairline. Give the list a fixed `width` (`sm`, `md` or `lg`) and leave the reading pane on `fill`. Start each pane with a `LayoutPaneHeader`: they share one height, so their bottom rules line up across the seams.

### What is SplitLayout for, and how do I choose which side the media sits on?

`SplitLayout` is a second shell for full-viewport auth, onboarding, and marketing screens that splits into two columns at `lg`. Which side `SplitMedia` sits on is just DOM order, and the `ratio` prop (`even`, `start`, or `end`) divides the columns.

## Exports and dependencies

`npx koalaui-cli add layout` writes `components/ui/layout/`. Import from `@/components/ui/layout`:

- Components and helpers: `Layout`, `LayoutTopbar`, `LayoutBody`, `LayoutRail`, `LayoutSidebar`, `LayoutSidebarTrigger`, `useLayoutSidebar`, `LayoutContent`, `LayoutPane`, `LayoutPaneHeader`, `LayoutContainer`, `LayoutAside`, `LayoutHeader`, `LayoutMobileBar`, `LayoutMobileSidebar`, `PageHeader`, `PageHeaderContent`, `PageHeaderHeading`, `PageHeaderDescription`, `PageHeaderActions`, `SplitLayout`, `SplitPane`, `SplitPaneBody`, `SplitMedia`, `SplitMediaOverlay`, `layoutVariants`, `splitLayoutVariants`
- Types: `LayoutProps`, `LayoutSidebarProps`, `LayoutSidebarTriggerProps`, `LayoutPaneProps`, `LayoutContainerProps`, `LayoutHeaderProps`, `LayoutMobileSidebarProps`, `SplitLayoutProps`, `SplitPaneBodyProps`
- Koala components it installs with it: `drawer`, `navbar`, `sidebar`
- Koala lib helpers it uses: `create-context`, `density`, `tv`, `utils`
- npm packages: `@phosphor-icons/react`
